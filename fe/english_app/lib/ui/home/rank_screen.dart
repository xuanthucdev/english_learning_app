import 'package:flutter/material.dart';

class RankScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Aptitude Test")),
      body: Center(child: Text("Nội dung Aptitude Test")),
    );
  }
}
